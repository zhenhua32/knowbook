# KnowBook 发布与签名说明

本文面向仓库维护者，说明当前发布 workflow 依赖的 GitHub Secrets、证书格式、注入方式，以及正式发版前需要完成的最小检查项。当前公开发布链路已经通过 `v0.1.2` 跑通；若需要完整的 tag 发版步骤与验收流程，见 [首个正式发版流程.md](首个正式发版流程.md)。

## 1. 当前发布链路

当前仓库的发布链路由下面几部分组成：

- 本地脚本：`npm run pack`、`npm run dist`、`npm run release`
- 发布预检：`npm run release:preflight`、`npm run release:verify-local`
- 打包配置：`electron-builder.yml`
- CI workflow：`.github/workflows/release.yml`
- 图标资源：`build/icon.png`
- macOS entitlements：`build/entitlements.mac.plist`

当前行为如下：

- 手动触发 workflow 时，会执行校验并产出安装包构建物
- 推送 `v*` tag 时，会调用 `npm run release` 并发布到 GitHub Releases
- 如果没有配置签名相关 secrets，workflow 仍会继续执行，产出 unsigned 安装包
- `electron-builder.yml` 当前应保持 `publish.releaseType: release`，否则 GitHub 只会生成隐藏 draft release，公开 release 页面不会出现安装包
- 预检脚本会在本地或 CI 中校验 `package.json` 版本号、tag 一致性、关键发布文件存在性，以及当前平台是否已经产出 release 构建物

## 2. GitHub Secrets 清单

### 2.1 Windows 代码签名

需要的 secrets：

- `WINDOWS_CERTIFICATE_PFX_BASE64`
- `WINDOWS_CERTIFICATE_PASSWORD`

含义：

- `WINDOWS_CERTIFICATE_PFX_BASE64`：PFX 证书文件的 Base64 文本，不是文件路径，不要包含多余换行
- `WINDOWS_CERTIFICATE_PASSWORD`：导出 PFX 时设置的密码

workflow 中的使用方式：

- Windows runner 会把 Base64 还原成临时 `pfx` 文件
- 还原后写入 `WIN_CSC_LINK` 和 `WIN_CSC_KEY_PASSWORD`
- electron-builder 再读取这两个环境变量执行签名

### 2.2 macOS 代码签名

需要的 secrets：

- `MACOS_CERTIFICATE_P12_BASE64`
- `MACOS_CERTIFICATE_PASSWORD`

含义：

- `MACOS_CERTIFICATE_P12_BASE64`：P12 证书文件的 Base64 文本，不是文件路径，不要包含多余换行
- `MACOS_CERTIFICATE_PASSWORD`：导出 P12 时设置的密码

workflow 中的使用方式：

- macOS runner 会把 Base64 还原成临时 `p12` 文件
- 还原后写入 `CSC_LINK` 和 `CSC_KEY_PASSWORD`
- electron-builder 会使用这两个环境变量进行 macOS 签名

### 2.3 macOS notarization

需要的 secrets：

- `APPLE_ID`
- `APPLE_APP_SPECIFIC_PASSWORD`
- `APPLE_TEAM_ID`

含义：

- `APPLE_ID`：用于 notarization 的 Apple ID 邮箱
- `APPLE_APP_SPECIFIC_PASSWORD`：Apple ID 的 app-specific password，不是登录密码
- `APPLE_TEAM_ID`：Apple Developer Team ID

workflow 中的使用方式：

- 仅在 macOS runner 且三个 secrets 都存在时注入
- 注入后供 electron-builder notarization 流程使用

## 3. 如何准备证书内容

### 3.1 Windows PFX 转 Base64

在 PowerShell 中执行：

```powershell
$bytes = [System.IO.File]::ReadAllBytes('C:\path\to\signing-cert.pfx')
[Convert]::ToBase64String($bytes) | Set-Clipboard
```

把剪贴板内容粘贴到 GitHub Secret `WINDOWS_CERTIFICATE_PFX_BASE64`。

### 3.2 macOS P12 转 Base64

在 macOS 或 Linux shell 中执行：

```bash
base64 -i /path/to/signing-cert.p12 | tr -d '\n'
```

如果是在 PowerShell 中处理 `.p12`，也可以直接用：

```powershell
$bytes = [System.IO.File]::ReadAllBytes('C:\path\to\signing-cert.p12')
[Convert]::ToBase64String($bytes) | Set-Clipboard
```

把结果填入 GitHub Secret `MACOS_CERTIFICATE_P12_BASE64`。

## 4. 在 GitHub 中配置 Secrets

进入仓库：

1. 打开 `Settings`
2. 进入 `Secrets and variables`
3. 进入 `Actions`
4. 选择 `New repository secret`
5. 按照本页的名称逐个创建

建议至少分成两组配置：

- 最小发布配置：只填 `GITHUB_TOKEN` 默认权限，允许 unsigned 构建
- 正式签名配置：补齐 Windows 或 macOS 的证书 secrets

如果要跑完整 macOS notarization，再补 Apple 三项凭据。

## 5. 当前仓库中的签名接线点

### 5.1 Workflow

`.github/workflows/release.yml` 中已经包含：

- Windows 证书还原步骤
- macOS 证书还原步骤
- Apple notarization 环境变量注入步骤
- 在 secrets 缺失时跳过签名准备步骤的条件判断

### 5.2 electron-builder

`electron-builder.yml` 中已经包含：

- Windows / macOS / Linux 图标设置
- GitHub Releases 发布目标
- macOS `hardenedRuntime`
- `build/entitlements.mac.plist` 的 entitlements 配置

### 5.3 macOS entitlements

当前 `build/entitlements.mac.plist` 主要为 Electron 运行时保留以下能力：

- `com.apple.security.cs.allow-jit`
- `com.apple.security.cs.allow-unsigned-executable-memory`
- `com.apple.security.cs.disable-library-validation`

这是当前 Electron 桌面应用更稳妥的默认签名起点；如果后续接入更严格的沙箱或分发要求，再单独收紧。

## 6. 推荐配置顺序

1. 先不配置任何签名 secrets，手动触发 workflow，确认 unsigned 构建产物正常上传。
2. 只补 Windows PFX secrets，验证 Windows 安装包签名。
3. 再补 macOS P12 secrets，验证 macOS 签名。
4. 最后补 Apple notarization 三项凭据，验证 macOS notarization。

这样能把问题限定在单个平台，不会在第一次接线时同时混入三类错误。

## 7. 正式发版前的检查项

至少确认这些项目：

0. 先运行 `npm run release:preflight`；如果要在本机顺手做一次完整 unsigned 预演，则运行 `npm run release:verify-local`。
1. `npm run pack` 在本地可通过。
2. workflow 手动触发时能上传构建产物。
3. Windows 安装包下载后能正常启动，且签名信息可见。
4. macOS 安装包能完成签名；如果启用 notarization，还要确认 Gatekeeper 不再拦截。
5. GitHub Release 中的产物命名符合当前 `artifactName` 规则。
6. 当前版本号与 tag 一致，例如 `package.json` 为 `x.y.z` 时，对应 tag 应为 `vx.y.z`。

## 8. 当前仍待补齐的部分

这份说明只解决“如何接线 secrets”问题，以下事项仍需后续补齐：

- 正式证书采购或导出流程
- 签名发版的人工验收记录
- 应用内自动更新的正式 GitHub Releases 端到端验收（当前代码与元数据产物已接线）
- 更完整的发布回滚策略
- 如需上架商店时的额外签名或审核流程

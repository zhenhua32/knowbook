# KnowBook 正式发版流程

本文面向仓库维护者，整理 `v*` tag 正式发版的实际操作步骤、验收 checklist 与回滚要点。这个文件最初用于首个正式发版整理，现继续作为后续正式发版的通用流程维护；证书与 GitHub Secrets 的配置方式见 [发布与签名说明.md](发布与签名说明.md)。

## 1. 适用范围

本流程适用于当前这条发布链路：

- 本地构建脚本：`npm run pack`、`npm run dist`、`npm run release`
- 打包器：`electron-builder`
- CI：`.github/workflows/release.yml`
- 发布目标：GitHub Releases

当前默认约定：

- 发布 tag 格式为 `v<package.json version>`，例如 `vx.y.z`
- workflow 在 `v*` tag 推送时执行正式发布
- 如果未配置签名相关 secrets，仍可执行 unsigned 发布

## 2. 发版前准备

### 2.1 确认版本号

先检查以下两件事：

1. `package.json` 中的版本号已经更新到本次目标版本。
2. 本次准备推送的 tag 与版本号一致，例如版本是 `x.y.z`，tag 就应为 `vx.y.z`。

### 2.2 确认发布凭据状态

根据这次发版目标，选择下面三种状态之一：

1. unsigned 验证发布：不配置任何签名 secrets，只验证产物、发布链路和安装流程。
2. 单平台签名发布：只配置 Windows 或 macOS 的签名 secrets，验证对应平台。
3. 完整签名发布：Windows 签名、macOS 签名和 notarization 全部启用。

如果这是第一次正式发版，建议先走“单平台签名发布”或“unsigned 验证发布”，不要第一次就同时排查三类证书问题。

### 2.3 本地预检查

正式打 tag 之前，建议至少在本地做完这些检查：

1. 运行 `npm run release:preflight`
2. 运行 `npm run typecheck`
3. 运行 `npm test`
4. 运行 `npm run pack`
5. 确认 `release/win-unpacked` 目录已经生成
6. 如本次修改涉及图标、签名或安装包配置，至少手动启动一次打包后的应用

## 3. 正式发版步骤

### 3.1 合并待发版变更

确认本次发版所需的代码、文档、workflow 与构建配置已经全部合并到目标分支。

最小要求：

- 发布脚本可用
- workflow 可用
- 版本号正确
- 当前工作区没有意外的临时改动

### 3.2 创建并推送 tag

建议顺序如下：

1. 提交发版前最后一次版本相关改动。
2. 创建带注释 tag。
3. 推送主分支。
4. 推送该 tag。

可参考以下命令：

```powershell
git add .
git commit -m "release: prepare v<version>"
git tag -a v<version> -m "KnowBook v<version>"
git push origin main
git push origin v<version>
```

如果默认分支不是 `main`，把命令中的分支名替换成实际分支名。

### 3.3 观察 GitHub Actions

推送 tag 后，进入 GitHub Actions，检查 `.github/workflows/release.yml` 对应的运行。

至少确认：

1. `validate` job 通过
2. `package` matrix 中 Windows、macOS、Linux 三个平台都启动成功
3. 如配置了 secrets，对应平台的“Prepare ... certificate”步骤被执行，而不是被跳过
4. `Publish tagged release` 步骤成功
5. `Upload build artifacts` 成功

### 3.4 检查 GitHub Release

workflow 完成后，进入 GitHub Releases，确认：

1. 已生成本次 tag 对应的 Release
2. Release 已公开发布，而不是停留在隐藏 draft 状态
3. 附件中包含预期平台的安装包或目录产物
4. 产物命名符合 `electron-builder.yml` 中的 `artifactName`
5. Release 标题和 tag 匹配

## 4. 验收 Checklist

### 4.1 构建侧

发布完成后至少验收以下项目：

1. Windows 构建物存在
2. macOS 构建物存在
3. Linux 构建物存在
4. GitHub Release 中每个平台的文件名与版本号一致
5. 构建物不是旧版本缓存产物

### 4.2 安装与启动侧

至少手动验证一个主平台，建议按优先级：Windows > macOS > Linux。

检查项：

1. 安装包可以正常下载
2. 安装或解压过程无异常
3. 应用可以正常启动
4. 首页、文档页、Database、图谱、AI、插件、设置页都能正常打开
5. 现有本地数据目录不会因安装包切换而丢失

### 4.3 签名侧

如果本次启用了签名，还要额外确认：

1. Windows 安装包或可执行文件能看到有效签名信息
2. macOS 应用签名有效
3. 如果启用了 notarization，Gatekeeper 不再拦截
4. 没有因为签名导致原生模块加载失败

### 4.4 功能冒烟侧

建议至少做一轮轻量功能冒烟：

1. 创建文档
2. 编辑并保存块内容
3. 触发一次 Markdown 备份
4. 打开 Database 页面
5. 打开插件中心
6. 打开设置页并确认 AI 配置区域正常显示

## 5. 建议记录项

每次正式发版都建议额外保留一份人工记录，至少包含：

- 发版时间
- 发版 tag
- 对应 commit SHA
- 本次是否启用 Windows 签名
- 本次是否启用 macOS 签名
- 本次是否启用 notarization
- 验收人
- 已验证的平台
- 已知剩余问题

这样后续出现安装、签名或回滚问题时，能快速追到第一次正式发版的基线。

## 6. 回滚建议

如果正式发版出现明显问题，建议按影响等级处理：

### 6.1 仅 Release 文案或附件错误

可以直接：

1. 保留 tag
2. 删除错误附件
3. 重新上传正确附件，或重新跑 workflow

### 6.2 构建产物有严重问题但 tag 已发布

建议：

1. 先删除有问题的附件，必要时在 Release 标题或描述里明确暂停下载
2. 记录问题根因
3. 修复后发下一个版本 tag，例如 `v0.1.1`

一般不建议在已经公开分发后直接复用原 tag 覆盖新产物，除非团队内部已明确允许这种做法。

### 6.3 签名或 notarization 问题

建议把问题拆开处理：

1. 先确认构建本身没问题
2. 再确认证书还原是否正确
3. 再确认密码是否匹配
4. 最后确认 Apple 凭据或 Team ID 是否正确

不要同时改 workflow、builder 配置和证书内容，否则第一次排错会失去定位能力。

## 7. 推荐分阶段执行方式

如果你现在要继续沿用当前正式发版链路，建议顺序是：

1. 先运行 `npm run release:verify-local`，确认本地 unsigned 分发构建物与命名规则都通过预检。
2. 手动触发 workflow 做一次 unsigned 构建验证。
3. 配置 Windows 签名 secrets，再做一次 tag 发版验证。
4. 最后再补 macOS 签名与 notarization。

这条路径最稳，因为它把“发布链路正确”“安装包可用”“签名可用”“notarization 可用”拆成了四个可独立验证的阶段。
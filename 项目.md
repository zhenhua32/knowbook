# KnowBook - 知识管理客户端项目规划

## 1. 产品定位
一款下一代的本地知识管理客户端，融合了块级排版与多维表格（类似 Notion）、网状双链结构（类似 Obsidian），以及深度集成的 AI Agent 自动化管理。

## 2. 核心架构决策
- **产品形态**：基于块级（Block-based）的文档编辑器，支持灵活排版与嵌套。
- **存储方案 (Local-first)**：
  - **核心数据库**：**SQLite**（用于处理极其复杂的块关联、双向链接引用及 Database 多维表格关系）。
  - **备份与便携性**：后台服务自动将 SQLite 数据序列化，定期导出为 **树形目录结构** 的 Markdown 文件。
- **UI/应用层框架**：**Electron (Node.js) + React + TypeScript**，利用成熟的前端生态和本地系统能力。
- **AI 智能集成**：
  - 首发版本不支持庞大的本地大模型，完全通过 **Cloud API** 提供支持（支持配置兼容 OpenAI 格式的各类云端/本地中转模型 API）。
  - 支持 **RAG (检索增强生成)** 与 **Agent 事件驱动工作流**。

## 3. 研发阶段拆解

### Phase 1: 客户端底层与基建
- [x] 初始化 Electron + React + TS 项目骨架。
- [x] 接入并配置 SQLite3 作为本地核心数据库。
- [x] 建立内部存储的 Schema（文档元数据、Block 节点、边关系等）。
- [x] 开发后台定时备份模块（SQLite -> 树形多级 Markdown 写入硬盘）。

#### 当前已落地（持续更新，现已覆盖 Phase 1 与部分 Phase 2）
- 已完成 Electron 主进程、Preload、Renderer 三层基础结构。
- 已接入 SQLite 本地库，初始化了 documents、blocks、links、app_settings 四张核心表。
- 已完成首版 Markdown 备份服务，按树形目录导出 `.md` 文件。
- 已提供首页仪表盘，可直接查看数据库路径、备份目录、文档计数和 AI API 配置。
- 已提供工作区文档树，可浏览根文档与子文档层级。
- 已提供单文档详情预览，可查看 summary、blocks、children、outgoing links、backlinks。
- 已支持创建根文档与子文档，创建后会自动刷新树并切换到新文档。
- 已支持文档详情编辑模式，可修改标题、摘要与块内容。
- 已打通文档保存链路（Renderer -> Preload IPC -> Main -> SQLite），保存后树与详情自动刷新。
- 已支持删除文档（保留并重挂子文档），并自动刷新当前选择状态。
- 已支持文档重挂父级（Move），并带有防循环校验（禁止移动到自身子树）。
- 已支持 AI 设置持久化（enabled/baseUrl/model）。
- 已支持文档树拖拽重挂父级（含根级 Drop 区和拖拽高亮反馈）。
- 已支持 AI API Key 保存与文档上下文问答（主进程代发请求，兼容 OpenAI 风格接口）。
- 已支持保存时解析 `[[文档名]]` / `[[路径]]` 并自动重建 links 表。
- 已支持编辑态引用建议面板，可在输入 `[[` 后提示候选文档并一键补全。
- 已支持只读预览中的内部链接点击跳转。
- 已支持工作区知识图谱视图（节点/边 SVG 可视化，点击节点跳转文档）。
- 已支持文档数据库表格视图（全量文档、搜索过滤、行点击联动）。
- 已支持基础 Board 视图（按父级 bucket 分组展示文档卡片）。
- 已支持 Board 视图列级拖拽重挂：文档卡片可直接拖到 Root 或现有父级列中，复用文档移动链路完成 reparent。
- 已让 Markdown 导出链路开始消费真实块树关系：后台备份、复制为 Markdown 与导出 Markdown 文件会先根据 `parent_block_id` 回推有效层级，再序列化列表/Todo 缩进，而不是只信任 `depth`。
- 已让编辑器内部 subtree / sibling helper 开始消费真实块树关系：子树边界、前后兄弟定位、多块 sibling range 校验与多块移动扫描都会优先依据 `parent_block_id`，而不是只按 `depth` 粗略推断。
- 已支持编辑态块拖拽排序，保存后按当前顺序持久化到 SQLite。
- 已支持编辑态块快捷插入，可在当前块上方/下方新增块，并支持 Ctrl/Cmd + Enter 在下方插入。
- 已支持编辑态块 Slash Command，可通过 `/` 执行块类型切换、上下插入、上移下移、删除，并支持上下键选择、Tab/Enter 确认、Escape 关闭。
- 已支持编辑态 Markdown 快捷输入，可通过 `# `、`## `、`> `、`- `、`1. `、`- [ ] `、`---`、``` 前缀快速切换块类型。
- 已新增 Quote、Bulleted List、Numbered List、Divider 四种块类型，并支持预览渲染与 Markdown 备份导出。
- 已支持块复制与块分裂：可通过 Slash Command / Duplicate 按钮 / Ctrl(Cmd)+Shift+D 复制当前块，并通过 Alt+Enter 按光标位置分裂当前块。
- 已支持块级智能输入行为：Enter 可续写标题/列表/Todo，Backspace 在块首可将标题/引用/列表/Todo 降级为普通段落。
- 已支持 Todo 块真实勾选状态：`checked` 已贯通 shared contract、SQLite schema/migration、store 读写、Markdown 导出以及编辑/预览 UI，支持 `- [ ] ` 与 `- [x] ` 快捷输入并持久化。
- 已支持 Math Formula 块：可通过 Slash Command、块类型切换或 `$$ ` 快捷输入创建，预览态使用 KaTeX 渲染，Markdown 备份导出为 `$$ ... $$` 公式块。
- 已支持 Todo / Bulleted List / Numbered List 的持久化缩进层级：`depth` 已贯通 shared contract、SQLite schema/migration、store、编辑态与 Markdown 导出，支持 Tab / Shift+Tab 与按钮进行缩进调整。
- 已补齐嵌套块交互细节：Slash Command 新增 Indent / Outdent；对嵌套 Todo / List 块在块首按 Backspace 会先减少一级缩进，空块按 Enter 也会先 outdent，再继续退出为普通段落。
- 已支持拖拽时调整嵌套层级：对 Todo / Bulleted List / Numbered List 块拖拽排序时，可通过左右拖动同时改变目标缩进深度，形成“换顺序 + 调层级”的单次操作。
- 已开始把 `depth` 演进为真实块树数据：`parent_block_id` 已接入 shared contract 与 store 读写，保存文档时会按当前块顺序和深度自动计算父子关系，并对已有数据库执行一次父子关系回填。
- 已在 renderer 中开始消费真实块树关系：`DocumentBlock` 已暴露 block `id`，只读预览下新增 Saved hierarchy 树形面板，按 `id + parentBlockId` 渲染块树结构。
- 已让编辑态开始消费块树语义：新增 Draft hierarchy 实时树形面板，按当前草稿顺序与 `depth` 预演保存后的块树结构，便于在编辑时直接观察层级结果。
- 已让编辑态草稿保留稳定 block id，并在 renderer/main 保存链路持续维护 `parentBlockId`；保存既有块时不再整体重置身份，Draft hierarchy 也开始直接消费草稿块树关系。
- 已让同级插入开始遵循块树语义：对嵌套 Todo / Bulleted List / Numbered List 执行 `+ Above` / `+ Below`、Ctrl/Cmd + Enter 或 Slash `insert above/below` 时，会插入同层级 sibling，而不是错误掉回根级 paragraph。
- 已让单块 Duplicate / Remove 也具备子树语义：对带子块的父块执行 Duplicate、Remove 或 Slash duplicate/delete 时，会整体复制或删除连续子树，而不是只处理根行导致子块悬空。
- 已让 Slash `Move Up` / `Move Down` 切到同层级 sibling 子树语义：移动父块时会整体跨过相邻兄弟子树，而不再只挪动单独根行。
- 已把单块子树移动入口统一到同一语义：编辑态下可用 Alt + ArrowUp / ArrowDown、块工具栏 `Move Up` / `Move Down`，以及“只选中 1 个块”时的连续选区工具栏移动按钮，直接移动当前 sibling 子树，并与 Slash move-up/move-down 共用同一路径。
- 已让“只选中 1 个父块”时的连续选区 Copy / Cut / Duplicate / Delete 进入子树语义：工具栏与键盘复制/剪切事件都会自动扩到整段连续子树，避免只处理根行后留下悬空子块。
- 已让“只选中 1 个父块”时的连续选区 Paste Replace 也进入子树语义：向被选中的父块粘贴多行或结构化块时，会整体替换其连续子树，而不是只替换根行后留下旧子块。
- 已让块级层级操作具备“子树语义”：Indent / Outdent 和拖拽重排不再只作用于当前块，而会连同连续子树一起移动，并避免把块拖进自身子树内部造成非法结构。
- 已支持真正的子块插入：编辑态新增 `/child` 与 `+ Child` 操作，可在当前块子树末尾追加一个嵌套子块，而不是只能插入同级块。
- 已补强拖拽层级预览：拖拽块时会显式显示 Drop before/after、目标 depth 与目标父块提示，不再只靠行高亮和缩进变化猜测最终树位置。
- 已支持连续多块选择与批量移动：编辑态可先 Select，再通过 Shift + Select 扩展连续选区，并对选中范围整体上移/下移。
- 已支持跨块粘贴与选区替换：在编辑态向单个块内粘贴多行文本时，会自动拆成多个块插入；若当前存在连续多块选区，则粘贴会直接替换整段选区，并沿用现有 markdown 前缀规则识别标题、Todo、列表、引用、公式、代码块等类型。
- 已支持连续选区的跨块复制/剪切：选中多个块后可通过工具栏或 Ctrl/Cmd + C/X 复制、剪切整段块序列，并以可回贴的 markdown/block 序列写入剪贴板。
- 已支持连续选区删除：选中多个块后可直接从选区工具栏删除整段块切片，并自动把焦点落回剩余邻近块。
- 已支持连续选区批量复制副本：选中多个块后可通过工具栏或 Ctrl/Cmd + Shift + D 在原切片后方整体复制一份，并自动切换选中到新副本。
- 已支持通过键盘 Delete / Backspace 删除连续选区：当连续多块选区处于激活状态时，可直接用键盘删除整段切片，而不会干扰单块文本编辑与现有 Backspace 降级逻辑。
- 已支持区分复制为块序列与复制为纯文本：连续选区工具栏现提供 `Copy Blocks` 与 `Copy Text` 两条路径，分别面向应用内可还原块结构的粘贴与外部编辑器纯文本粘贴。
- 已支持通过键盘 Alt + ArrowUp / ArrowDown 移动连续选区：当连续多块选区处于激活状态时，可直接用纯键盘将整段切片上移/下移，而不必依赖工具栏按钮或拖拽。
- 已支持通过键盘 Tab / Shift+Tab 调整连续选区缩进：当连续多块选区处于激活状态时，可直接用纯键盘整体缩进/反缩进其中所有可嵌套块，并保持相对层级结构不被打乱。
- 已支持连续选区批量转换块类型：连续多块选区工具栏现提供 `Convert to...`，可将整段切片统一转换为 Text / Todo / Quote / Bullet / Numbered 等共享块类型，并沿用现有块类型归一化逻辑处理 `checked` 与 `depth`。
- 已跑通 `npm install` 与 `npm run build`，当前项目可以完成类型检查与打包。
- 已让多块连续选区的移动操作具备树感知：移动多块选区时，会先验证所有块是否为有效的"兄弟范围"（相同 depth）；若不是则禁止移动；若是，则只在同层级兄弟之间移动，避免跨越不同父块或破坏树结构。
- 已为块分裂操作添加树边界保护：当分裂一个有子块的父块时，会拒绝分裂并输出警告，防止子块孤立。
- 已为粘贴操作添加树深度感知：粘贴结构化块（多行 markdown）到特定深度时，会自动调整粘贴块的缩进深度，使其与目标块保持正确的相对关系，而不是无视上下文深度。
- 已为拖拽重排添加树边界保护：拖拽块时会验证不将块拖入自身子树内、不拖到无效的重复位置、并对无效深度进行钳制和警告。
- 已为缩进/反缩进操作添加合法性验证：缩进时会检查前驱块是否存在且深度足够作为新父块；多块选区缩进时会验证最小深度与前驱块的相对关系，防止产生悬空的无效树结构。
- 已为多块删除操作添加子树提升机制：删除一个块范围时，如果该范围后面的块深度大于被删除范围最后一块的深度，说明它们是该块的子块，会被自动提升到正确的父层级深度，而不是留下孤立的深层块。
- 已为块类型转换添加子树保护：当尝试将一个可嵌套块（Todo/List）转换为非嵌套块（Paragraph/Heading/Code）时，如果该块有子块，会拒绝转换并输出警告，防止产生无效的树结构。
- 已为多块选区添加连贯性检查和提示：选区工具栏会检测选中的块是否都在同一深度；如果混合不同深度的块，会显示警告标记（⚠ Mixed depths）并提示用户某些操作可能行为异常。
- 已为保存时添加块树结构验证：在保存文档前，会检验所有块的深度有效性、类型与嵌套关系的兼容性，以及不存在不合理的深度跳跃；如果验证失败会阻止保存并显示详细错误信息。
- 已为多块操作添加树边界自动扩展：当进行复制、剪切、删除、重复等多块操作时，如果选区跨越有子块的块，会自动扩展选区以包含完整子树，确保树结构完整性；所有键盘快捷键、右键菜单和拖拽操作都使用统一的范围扩展逻辑。
- 已为多块缩进添加递进关系验证：当对多块选区进行缩进时，会检查选区内每个块经过缩进后是否都有合法的前驱块支持其新的深度；如果某个块无法找到充分深度的前驱块，整个操作会被阻止，防止破坏树结构一致性。
- 已为多块拖拽添加支持和目标验证：当用户对多块选区进行拖拽时，系统会识别拖拽的是多块范围，并在落地时验证所有块在目标位置是否都能保持有效的树结构；特别检查每个块的前驱块深度是否充分，防止形成悬空块。
- 已为粘贴块添加树结构规范化：粘贴多块时会检查块之间的相对深度，修复任何深度跳跃现象（如块1深度为0，块2深度为2的情况）；确保每个块的深度最多比前驱块深1，形成有效的树结构。
- 已为保存时添加块ID唯一性检查：保存文档前会检查所有块的ID是否唯一；如发现重复ID，会报告错误并阻止保存，防止数据一致性问题。
- 已建立非嵌套块层级规范化的保障：确保所有非嵌套块（paragraph、heading、quote、code等）在任何操作中的深度始终为0；normalizeDraftBlocks和normalizeBlockDepth确保类型与深度的对应关系始终有效。
- 已实现块合并功能：在块首按 Backspace 时会合并到上一个块；合并前检查前驱块是否存在子块，防止破坏树结构；只允许相同类型和深度的块合并，确保内容和结构的一致性。
- 已实现块搜索/快速跳转功能：编辑态下可通过 Cmd/Ctrl+F 打开块搜索面板；支持按块内容和块类型实时搜索；点击搜索结果可直接跳转到该块；搜索面板可用 Escape 关闭。
- 已实现块引用/块链接核心能力：预览态下支持 `[[blockId]]` 语法识别块引用；建立块引用解析映射系统 buildBlockReferencesMap；块引用显示为带有特殊样式的内联元素 .inline-link-block，与文档链接 .inline-link 区分；编辑态下在 `[[` 补全中同时展示同文档块建议和文档链接建议，支持快速插入块引用；块建议通过 getBlockSuggestionsForLink() 进行查询和过滤，限制为最多 5 条。
- 已实现块内容 Markdown 样式支持：预览态下支持 `**粗体**`、`__粗体__`、`*斜体*`、`_斜体_`、`` `代码` ``、`~~删除线~~` 等 markdown 样式；创建 parseMarkdownStyles() 函数将内容解析成样式段落，renderStyledContent() 函数将样式渲染为对应的 HTML 元素（`<strong>`、`<em>`、`<code>`、`<del>`）；添加 .inline-code 样式用于代码显示；所有样式与块链接补全兼容，内容中混合 markdown 样式和链接都能正确渲染。
- 已实现块标签系统：在 DocumentBlock 和 DocumentBlockDraft 中添加 tags 字段支持；编辑态下可通过 "+ Tag" 按钮添加标签，通过标签徽章右侧的 × 删除标签；支持 updateBlockTags()、addBlockTag()、removeBlockTag()、getAllBlockTags() 等标签管理函数；预览态和编辑态都显示块标签徽章，标签使用蓝色背景与块内容区分；为后续块标签过滤和分组功能奠定基础。
- 已实现块标签过滤系统：编辑态下显示 "Filter by tags" 面板，列出当前文档中所有使用过的标签及其块数计数；用户可点击标签进行筛选，支持多标签 OR 过滤（任意标签匹配即显示）；活跃标签以蓝色高亮显示，面板显示当前过滤结果数；支持"Clear filters"按钮快速清空所有过滤条件；过滤影响编辑器显示但不改变保存的块结构。
- 已实现块折叠/展开功能：编辑态下，有子块的父块左侧显示 ▼/▶ 折叠切换按钮；点击后可折叠整个子树（子块从显示列表中隐藏），再次点击展开；切换文档或取消编辑时自动清空折叠状态；折叠状态只影响编辑视图，不改变保存的块数据结构。
- 已实现代码块语言标注系统：代码块编辑态顶部新增语言选择器，支持 JavaScript、TypeScript、Python、Rust、Go、Java、C/C++、C#、HTML、CSS、JSON、YAML、SQL、Bash、Markdown 等常见语言；预览态代码块顶部显示语言标签徽章；语言信息持久化到 SQLite（language 列），Markdown 备份导出使用正确的语言前缀（如 ```python）；数据层通过 migration 向后兼容已有数据库。
- 已增强反向链接面板上下文展示：getDocumentDetail 的 backlinks 查询现在同时附带引用块的内容片段（最多 200 字符）；UI 中反向链接卡片展示引用上下文，以斜体左侧带竖线的样式呈现，帮助用户快速了解来源文档在哪里引用了当前文档；添加 contextSnippet 字段到 LinkedDocument 接口与 contracts，向后兼容（可选字段）。

### Phase 2: 核心编排引擎 (Block Editor)

#### 当前策略（2026-04-29）
- 继续沿现有自研 Block Editor 演进，短期内不引入 Lexical / ProseMirror。
- 当前重点从“具备基本编辑能力”转向“把块模型、嵌套交互和块树能力继续做深”。

- [x] 明确继续使用现有 Block Editor 路线，暂不切换到 Lexical / ProseMirror。
- [x] 开发区块（Block）数据模型、持久化字段与预览/编辑渲染器。
- [x] 实现核心块类型（文本、各级标题、TODO、代码块、数学公式、引用、列表、分割线等）。
- [x] 实现块拖拽重排与嵌套层级交互。
- [ ] 将当前 `depth` 模型进一步演进为真正的块树结构，衔接已存在的 `parent_block_id`。
- [ ] 补强更复杂的编辑器能力（多块选择、跨块粘贴/移动、更明确的拖拽层级预览等）。

### Phase 3: 网状知识与双向链接
- [ ] 实现编辑器层面的 `[[页面或块ID]]` 语法解析与补全。
- [ ] 底层构建双链的关系图谱。
- [ ] 开发反向链接 (Backlinks) 侧边栏/底栏面板。
- [ ] 建立可视化的全局 Knowledge Graph 视图。

### Phase 4: 多维表格体系 (Database)
- [ ] 开发动态列定义（Schema UI），映射到底层 SQLite 虚拟表或 JSON 结构。
- [ ] 实现核心字段类型（Text, Select, Multi-select, Date, Checkbox）。
- [ ] 完成 Table (表格) 视图的渲染。
- [x] 完成 Kanban (看板) 视图的拖拽与渲染。

### Phase 5: RAG 与 AI Agent 自动化
- [ ] 构建全局 AI 设置项与 API 请求包装层。
- [ ] 引入 SQLite 向量插件 (如 `sqlite-vss`) 或本地轻量向量库，实现对所有笔记的 Embedding 存储和语义重搜（RAG）。
- [ ] 构筑事件总线 (Event Bus)，实现 AI Agent 触发器（如：检测新建文档自动添加标签、根据状态更改自动触发整理等）。

### Phase 6: 插件生态系统
- [ ] 设计生命周期与 UI 钩子。
- [ ] 暴露一套安全的 JavaScript/TypeScript API，便于第三方无缝编写插件扩充功能。

---
*初始规划建立日期：2026-04-27*



# KnowBook - 知识管理客户端项目规划

## 1. 产品定位
一款下一代的本地知识管理客户端，融合了块级排版与多维表格（类似 Notion）、网状双链结构（类似 Obsidian），以及深度集成的 AI Agent 自动化管理。

## 2. 核心架构决策
- 产品形态：基于块级（Block-based）的文档编辑器，支持灵活排版与嵌套。
- 存储方案（Local-first）：
  - 核心数据库：SQLite，用于处理复杂的块关联、双向链接引用及 Database 多维表格关系。
  - 备份与便携性：后台服务自动将 SQLite 数据序列化，定期导出为树形目录结构的 Markdown 文件。
- UI / 应用层框架：Electron（Node.js）+ React + TypeScript。
- AI 智能集成：
  - 首发版本不支持庞大的本地大模型，完全通过 Cloud API 提供支持。
  - 支持兼容 OpenAI 风格的云端 / 本地中转模型 API。
  - 支持 RAG（检索增强生成）与 Agent 事件驱动工作流。

## 3. 当前状态总览（2026-05-16）
- 当前主线功能已经从“可用原型”进一步推进到“主链路稳定、持续收口”的阶段，Phase 1 至 Phase 6 的首版能力均已在代码中落地。
- 当前已具备：本地 SQLite 存储、树形 Markdown 备份、块编辑器与块树模型、双链与知识图谱、文档 catalog Table / Board 视图、独立 database / entity 基础模型与局部 UI、RAG、AI 自动摘要、首版插件宿主。
- 当前研发重心已从“补主功能”转向“明确产品边界、补稳定回归、继续深化数据库与插件生态能力”。
- 自动化测试已覆盖 shared 纯函数、event bus、schema、store 主链路与边界分支、backup exporter、plugin host 主流程与异常路径，以及 renderer 可单测模块（i18n 与 SSR 组件渲染）；tests/ 已形成 62 项通过的单测 / 集成回归基线，同时已引入 Playwright + Electron 的真实桌面端 E2E 基础设施，但稳定纳入 `npm run test:e2e` 的 Electron 用例仍较少。

## 4. 分阶段完成度

### Phase 1: 客户端底层与基建
- [x] 初始化 Electron + React + TS 项目骨架。
- [x] 接入并配置 SQLite3 作为本地核心数据库。
- [x] 建立内部存储的 Schema（文档元数据、Block 节点、边关系等）。
- [x] 开发后台定时备份模块（SQLite -> 树形多级 Markdown 写入硬盘）。

当前状态：已完成。

补充说明：
- 已完成主进程、Preload、Renderer 三层基础结构。
- 已落地 documents、blocks、links、databases、document_database_columns、document_database_values、database_entities、document_embeddings、workspace_events、app_settings 等核心表。
- 已完成 Markdown 备份导出链路，并可从桌面端手动触发。

### Phase 2: 核心编排引擎（Block Editor）
- [x] 明确继续使用现有 Block Editor 路线，暂不切换到 Lexical / ProseMirror。
- [x] 开发区块（Block）数据模型、持久化字段与预览 / 编辑渲染器。
- [x] 实现核心块类型（文本、标题、TODO、代码块、数学公式、引用、列表、分割线等）。
- [x] 实现块拖拽重排与嵌套层级交互。
- [x] 将当前 depth 模型进一步演进为真正的块树结构，衔接已存在的 parent_block_id。
- [x] 补强复杂编辑器能力（多块选择、跨块粘贴 / 移动、更明确的拖拽层级预览等）。

当前状态：已完成首版，后续以深化与收口为主。

补充说明：
- 编辑器已切到真实块树语义，保存前会校验 parentBlockId、父块类型、顺序与深度一致性。
- 已覆盖块复制、块分裂、块合并、块折叠、块搜索、块标签、标签过滤、代码块语言标注等深化能力。
- 多块选择、移动、缩进、删除、拖拽、粘贴都已经具备树结构保护与可见树语义。

### Phase 3: 网状知识与双向链接
- [x] 实现编辑器层面的 [[页面或块ID]] 语法解析与补全。
- [x] 底层构建双链关系图谱。
- [x] 开发反向链接（Backlinks）面板。
- [x] 建立可视化的全局 Knowledge Graph 视图。

当前状态：已完成首版主清单。

补充说明：
- 已完成页面级 [[文档名]] / [[路径]] 链接、同文档块级 [[blockId]] 引用，以及跨文档块引用 [[documentPath#blockId]] 的编辑态补全、保存解析、预览渲染与跳转闭环。
- 工作区图谱已同时消费树边与链接边。
- 反向链接面板已支持引用块内容上下文展示。
- 后续增强将主要集中在更细粒度的块级关系能力与引用语义扩展，而不是补齐基础闭环。

### Phase 4: 多维表格体系（Database）
- [x] 开发动态列定义（Schema UI），映射到底层 SQLite 表结构与字段值存储。
- [x] 实现核心字段类型（Text、Select、Multi-select、Date、Checkbox）。
- [x] 完成 Table（表格）视图的渲染。
- [x] 完成 Board（看板）视图的拖拽与渲染。

当前状态：已完成首版 catalog database，并已落地独立 database / entity 的基础模型与局部 UI。

补充说明：
- 已完成文档 catalog 的 Table 视图、自定义列定义、列管理、单元格编辑与搜索过滤。
- 已完成 Board 视图按父级 bucket、Select、Multi-select、Checkbox、Date、Text 字段分组，并支持拖拽改写对应字段值。
- 已落地 databases / database_entities 表结构、独立数据库创建、独立 entity 创建与字段编辑的首版界面。
- 当前数据库页仍同时承载 documents catalog 的结构化视图与独立 database entity 的基础 UI，距离完整独立的 Notion 风格 database 产品形态仍有收口工作。

### Phase 5: RAG 与 AI Agent 自动化
- [x] 构建全局 AI 设置项与 API 请求包装层。
- [x] 引入 Embedding 存储与本地语义检索，实现 RAG。
- [x] 构筑事件总线（Event Bus），承载 AI 自动化触发器。

当前状态：已完成首版主链路，当前自动化以摘要工作流为主。

补充说明：
- Renderer / Preload / Main 已打通 AI 设置、问答、语义检索和上下文组装链路。
- 已落地 OpenAI-compatible Embedding 存储、文档变更后的增量同步与本地 cosine 检索。
- 已落地 autoSummaryOnSave 自动化触发器，并支持对当前文档手动运行已启用的摘要自动化。
- 后续扩展将主要沿现有事件总线继续增加更多自动化工作流，而不是重新搭建另一套触发系统。

### Phase 6: 插件生态系统
- [x] 设计生命周期与 UI hooks。
- [x] 暴露首版安全 JavaScript API，支持第三方扩展。

当前状态：已完成首版插件宿主，并已具备基础版本兼容校验能力。

补充说明：
- 主进程会扫描工作区 plugins/ 与用户数据目录 plugins/ 下的插件目录。
- 插件运行于受限 vm 上下文，当前可订阅 workspace events、贡献 dashboard cards、贡献 document actions、读取文档详情、更新 summary、写入插件事件。
- 已完成插件启用 / 禁用、重扫、安装、卸载、替换升级、单插件 Reload / Recover。
- 已支持基于 `engines.knowbook` 的插件版本兼容校验。
- 当前仓库已带 activity-pulse 示例插件。
- 插件市场、打包分发、更多 renderer slot、外部可消费的 TypeScript SDK 仍待补强。

## 5. 已落地能力清单（按能力域整理）

### 5.1 工作区与存储
- Electron 桌面壳、Preload IPC、Renderer 主界面已经完整打通。
- SQLite 是单一事实源；Markdown 备份会定期导出为树形目录结构。
- 文档支持创建、删除、重挂父级、树形浏览与最近文档浏览。

### 5.2 编辑器与块树
- 已支持文本、标题、Todo、引用、列表、代码、公式、分割线等核心块类型。
- 已支持真实块树关系、层级校验、同级插入、子块插入、子树移动、子树复制 / 删除 / 替换。
- 已支持多块连续选区、键盘移动、键盘缩进、批量转换、跨块粘贴与树边界保护。
- 已支持块标签、标签过滤、折叠、搜索、代码块语言与 Markdown 样式渲染。

### 5.3 双链与图谱
- 已支持页面链接、同文档块引用、跨文档块引用、链接建议、只读跳转与保存后重建 links 表。
- 已支持文档详情中的 Outgoing Links / Backlinks 面板。
- 已支持工作区 Knowledge Graph 视图。

### 5.4 Database 视图
- 已支持 Text、Select、Multi-select、Date、Checkbox 五类字段。
- 已支持文档 catalog 的列新增、重命名、排序、删除、选项编辑与单元格持久化编辑。
- 已支持 Table 视图、搜索过滤、Board 视图与列内拖拽；Board 可按父级 bucket、Select、Multi-select、Checkbox、Date、Text 字段分组。
- 已支持独立 database 创建、独立 entity 持久化以及实体字段编辑；数据库页现已拆分为文档 catalog 与独立 database 两个显式视图，避免两套 schema 在同一主视图中混用。

### 5.5 AI 与自动化
- 已支持 AI 配置持久化、API Key 保存、文档问答与语义检索。
- 已支持 Embedding 缓存、文档变更后的增量刷新、模型变更后的回填与本地 cosine 检索。
- 已支持工作区事件流展示与摘要自动化触发。
- 已支持自动摘要与手动触发入口；当前自动化仍以摘要工作流为主，块高亮继续保留手动能力。

### 5.6 插件系统
- 已支持插件发现、沙箱执行、启用 / 禁用、安装 / 卸载、替换升级、Reload / Recover。
- 已支持首页 dashboard cards、文档级 actions 与 workspace events 订阅。
- 已支持插件版本兼容校验（engines.knowbook）。
- 已附带 activity-pulse 示例插件用于演示插件闭环。

## 6. 当前未收口项
- 独立的 Notion 风格 database 产品形态尚未完全收口；虽然数据库页已经拆分出文档 catalog 与独立 database 双视图，但独立 entity 的视图类型、批量操作和更强的 database 抽象仍需继续推进。
- 插件市场、打包分发、外部可消费的 TypeScript SDK 与更多 renderer UI slot 仍待补强。
- 稳定 Electron E2E 回归覆盖仍不足；块拖拽、多选、分裂 / 合并、复杂输入、知识网络、插件与 AI 自动化整链路仍缺少足够的 `@electron` 用例。
- AI 自动化当前仍以摘要工作流为主，更多事件驱动 enrichments 仍待扩展。
- Renderer 侧 App.tsx 体量较大，Database / AI / 插件管理等状态仍集中在单文件中，后续需要按领域拆分。

## 7. 下一步建议
1. 补稳定 E2E：优先把现有 Playwright 用例收口到真实 Electron 应用，并覆盖块拖拽、多选、数据库视图交互与 AI 自动化整链路。
2. 深化插件生态：优先补插件市场、打包分发、外部 Typed SDK 与更多 hook / renderer slot。
3. 深化 Database：明确 documents catalog 与独立 database 的产品边界，继续推进独立 entity 视图与更强的 Board / View 抽象。
4. 拆前端：把 App.tsx 按工作区、编辑器、Database、AI、插件管理与设置分层拆开。
5. 深化自动化与知识网络：在现有摘要工作流和跨文档块引用基础上继续扩展更多 AI 自动化与更细粒度的块级关系能力。

---
*初始规划建立日期：2026-04-27*

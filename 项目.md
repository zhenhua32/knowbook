# KnowBook - 知识管理客户端项目规划

## 1. 产品定位
一款下一代的本地知识管理客户端，融合了块级排版与多维表格（类似 Notion）、网状双链结构（类似 Obsidian），以及深度集成的 AI Agent 自动化管理。

## 2. 核心架构决策
- **产品形态**：基于块级（Block-based）的文档编辑器，支持灵活排版与嵌套。
- **存储方案 (Local-first)**：
  - **核心数据库**：**SQLite**（用于处理极其复杂的块关联、双向链接引用及 Database 多维表格关系）。
  - **备份与便携性**：后台服务自动将 SQLite 数据序列化，定期导出为 **树形目录结构** 的 Markdown 文件。
- **UI/应用层框架**：**Electron (Node.js) + React + TypeScript**，利用成熟的前端生态和本地系统能力。
- **AI 智能集成**：
  - 首发版本不支持庞大的本地大模型，完全通过 **Cloud API** 提供支持（支持配置兼容 OpenAI 格式的各类云端/本地中转模型 API）。
  - 支持 **RAG (检索增强生成)** 与 **Agent 事件驱动工作流**。

## 3. 研发阶段拆解

### Phase 1: 客户端底层与基建
- [x] 初始化 Electron + React + TS 项目骨架。
- [x] 接入并配置 SQLite3 作为本地核心数据库。
- [x] 建立内部存储的 Schema（文档元数据、Block 节点、边关系等）。
- [x] 开发后台定时备份模块（SQLite -> 树形多级 Markdown 写入硬盘）。

#### 当前已落地（持续更新，现已覆盖 Phase 1 与部分 Phase 2）
- 已完成 Electron 主进程、Preload、Renderer 三层基础结构。
- 已接入 SQLite 本地库，初始化了 documents、blocks、links、app_settings 四张核心表。
- 已完成首版 Markdown 备份服务，按树形目录导出 `.md` 文件。
- 已提供首页仪表盘，可直接查看数据库路径、备份目录、文档计数和 AI API 配置。
- 已提供工作区文档树，可浏览根文档与子文档层级。
- 已提供单文档详情预览，可查看 summary、blocks、children、outgoing links、backlinks。
- 已支持创建根文档与子文档，创建后会自动刷新树并切换到新文档。
- 已支持文档详情编辑模式，可修改标题、摘要与块内容。
- 已打通文档保存链路（Renderer -> Preload IPC -> Main -> SQLite），保存后树与详情自动刷新。
- 已支持删除文档（保留并重挂子文档），并自动刷新当前选择状态。
- 已支持文档重挂父级（Move），并带有防循环校验（禁止移动到自身子树）。
- 已支持 AI 设置持久化（enabled/baseUrl/model）。
- 已支持文档树拖拽重挂父级（含根级 Drop 区和拖拽高亮反馈）。
- 已支持 AI API Key 保存与文档上下文问答（主进程代发请求，兼容 OpenAI 风格接口）。
- 已支持保存时解析 `[[文档名]]` / `[[路径]]` 并自动重建 links 表。
- 已支持编辑态引用建议面板，可在输入 `[[` 后提示候选文档并一键补全。
- 已支持只读预览中的内部链接点击跳转。
- 已支持工作区知识图谱视图（节点/边 SVG 可视化，点击节点跳转文档）。
- 已支持文档数据库表格视图（全量文档、搜索过滤、行点击联动）。
- 已支持基础 Board 视图（按父级 bucket 分组展示文档卡片）。
- 已支持编辑态块拖拽排序，保存后按当前顺序持久化到 SQLite。
- 已支持编辑态块快捷插入，可在当前块上方/下方新增块，并支持 Ctrl/Cmd + Enter 在下方插入。
- 已支持编辑态块 Slash Command，可通过 `/` 执行块类型切换、上下插入、上移下移、删除，并支持上下键选择、Tab/Enter 确认、Escape 关闭。
- 已支持编辑态 Markdown 快捷输入，可通过 `# `、`## `、`> `、`- `、`1. `、`- [ ] `、`---`、``` 前缀快速切换块类型。
- 已新增 Quote、Bulleted List、Numbered List、Divider 四种块类型，并支持预览渲染与 Markdown 备份导出。
- 已支持块复制与块分裂：可通过 Slash Command / Duplicate 按钮 / Ctrl(Cmd)+Shift+D 复制当前块，并通过 Alt+Enter 按光标位置分裂当前块。
- 已支持块级智能输入行为：Enter 可续写标题/列表/Todo，Backspace 在块首可将标题/引用/列表/Todo 降级为普通段落。
- 已支持 Todo 块真实勾选状态：`checked` 已贯通 shared contract、SQLite schema/migration、store 读写、Markdown 导出以及编辑/预览 UI，支持 `- [ ] ` 与 `- [x] ` 快捷输入并持久化。
- 已支持 Math Formula 块：可通过 Slash Command、块类型切换或 `$$ ` 快捷输入创建，预览态使用 KaTeX 渲染，Markdown 备份导出为 `$$ ... $$` 公式块。
- 已支持 Todo / Bulleted List / Numbered List 的持久化缩进层级：`depth` 已贯通 shared contract、SQLite schema/migration、store、编辑态与 Markdown 导出，支持 Tab / Shift+Tab 与按钮进行缩进调整。
- 已补齐嵌套块交互细节：Slash Command 新增 Indent / Outdent；对嵌套 Todo / List 块在块首按 Backspace 会先减少一级缩进，空块按 Enter 也会先 outdent，再继续退出为普通段落。
- 已支持拖拽时调整嵌套层级：对 Todo / Bulleted List / Numbered List 块拖拽排序时，可通过左右拖动同时改变目标缩进深度，形成“换顺序 + 调层级”的单次操作。
- 已跑通 `npm install` 与 `npm run build`，当前项目可以完成类型检查与打包。

### Phase 2: 核心编排引擎 (Block Editor)

#### 当前策略（2026-04-29）
- 继续沿现有自研 Block Editor 演进，短期内不引入 Lexical / ProseMirror。
- 当前重点从“具备基本编辑能力”转向“把块模型、嵌套交互和块树能力继续做深”。

- [x] 明确继续使用现有 Block Editor 路线，暂不切换到 Lexical / ProseMirror。
- [x] 开发区块（Block）数据模型、持久化字段与预览/编辑渲染器。
- [x] 实现核心块类型（文本、各级标题、TODO、代码块、数学公式、引用、列表、分割线等）。
- [x] 实现块拖拽重排与嵌套层级交互。
- [ ] 将当前 `depth` 模型进一步演进为真正的块树结构，衔接已存在的 `parent_block_id`。
- [ ] 补强更复杂的编辑器能力（多块选择、跨块粘贴/移动、更明确的拖拽层级预览等）。

### Phase 3: 网状知识与双向链接
- [ ] 实现编辑器层面的 `[[页面或块ID]]` 语法解析与补全。
- [ ] 底层构建双链的关系图谱。
- [ ] 开发反向链接 (Backlinks) 侧边栏/底栏面板。
- [ ] 建立可视化的全局 Knowledge Graph 视图。

### Phase 4: 多维表格体系 (Database)
- [ ] 开发动态列定义（Schema UI），映射到底层 SQLite 虚拟表或 JSON 结构。
- [ ] 实现核心字段类型（Text, Select, Multi-select, Date, Checkbox）。
- [ ] 完成 Table (表格) 视图的渲染。
- [ ] 完成 Kanban (看板) 视图的拖拽与渲染。

### Phase 5: RAG 与 AI Agent 自动化
- [ ] 构建全局 AI 设置项与 API 请求包装层。
- [ ] 引入 SQLite 向量插件 (如 `sqlite-vss`) 或本地轻量向量库，实现对所有笔记的 Embedding 存储和语义重搜（RAG）。
- [ ] 构筑事件总线 (Event Bus)，实现 AI Agent 触发器（如：检测新建文档自动添加标签、根据状态更改自动触发整理等）。

### Phase 6: 插件生态系统
- [ ] 设计生命周期与 UI 钩子。
- [ ] 暴露一套安全的 JavaScript/TypeScript API，便于第三方无缝编写插件扩充功能。

---
*初始规划建立日期：2026-04-27*



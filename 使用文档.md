# KnowBook 使用文档

## 1. 产品说明
KnowBook 是一个本地优先的知识管理客户端，核心形态是“文档树 + 块编辑器 + 结构化数据库 + 知识图谱 + AI 自动化 + 插件宿主”。

当前版本适合以下场景：
- 个人知识库与项目笔记整理
- 具备层级结构的块级写作
- 为文档补充结构化字段并用 Table / Board 管理
- 基于本地 SQLite 的长期沉淀与 Markdown 备份
- 通过 AI 对文档做问答、检索和摘要增强

## 2. 启动方式
### 2.1 开发模式
在项目根目录执行：

```bash
npm install
npm run dev
```

### 2.2 构建产物
在项目根目录执行：

```bash
npm run build
```

构建完成后，Electron 主进程、Preload、Renderer 产物会输出到 out 目录。

### 2.3 打包桌面产物
如果你需要验证接近正式发布的桌面安装包行为，可在项目根目录执行：

```bash
npm run dist
```

安装包和解包目录会输出到 release 目录；如果希望连同发布预检一起完成一次本地 unsigned 验证，可执行：

```bash
npm run release:verify-local
```

## 3. 首次进入后先做什么
建议第一次打开后先完成下面 5 步：

1. 在左侧栏找到“界面语言 / Interface language”，切换为中文或 English。
2. 进入“配置中心 / Settings”，填写 Base URL、Model、Embedding model、API Key，并按需打开自动摘要。
3. 如果当前是正式打包版本，可在“配置中心 / Settings”查看应用更新状态，并手动执行一次检查更新。
4. 在工作区树中创建一篇根文档，进入编辑态开始录入内容。
5. 用“立即执行备份 / Run backup now”测试一次 Markdown 备份是否正常生成。

## 4. 界面结构
主界面大致分为两部分：

### 4.1 左侧侧栏
左侧主要负责页面导航与全局入口：
- 文档页
- 总览页
- Database 页
- 图谱页
- AI 助手页
- 插件中心页
- 配置中心页

### 4.2 主内容区
主内容区包含以下模块：
- 工作区总览与统计卡片
- 工作区文档树
- 文档预览 / 编辑面板
- Database 的 Table / Board / 独立 entity 视图
- 知识图谱
- AI 问答与语义检索结果
- 插件管理与配置面板
- 存储信息、语言、AI、应用更新与备份设置

## 5. 语言切换
当前版本已支持中文和英文界面切换。

操作方法：
1. 打开左侧侧栏顶部的“界面语言 / Interface language”。
2. 在下拉框中选择“中文”或“English”。
3. 选择后会立即生效，并自动保存到本地设置。
4. 下次重新打开应用时，会保留上次选择的语言。

## 6. 文档与工作区树
### 6.1 新建文档
- 点击工作区树右上角“新建根文档 / New root”可创建顶层文档。
- 在文档预览面板点击“新增子文档 / Add child”可创建当前文档的子文档。

### 6.2 移动文档
支持两种方式：
- 在工作区树中拖拽文档到另一个文档下，完成重挂父级。
- 在预览面板使用“移动到 / Move to”下拉框，把当前文档移动到指定父级或根目录。

### 6.3 收藏文档
在预览面板顶部点击星标按钮，可将文档加入收藏区，便于快速打开。

### 6.4 全局搜索
点击工作区树区域的搜索按钮，或使用快捷键 Ctrl+K：
- 可搜索文档标题
- 可搜索块内容
- 搜索结果可直接跳转到对应文档

## 7. 文档编辑器
### 7.1 基本编辑
进入编辑模式后，可以修改：
- 标题
- 摘要
- 文档中的所有块内容

### 7.2 块类型
当前支持的块类型包括：
- 文本段落
- 一级标题
- 二级标题
- 待办事项
- 代码块
- 数学公式
- 引用块
- 无序列表
- 有序列表
- 分隔线

### 7.3 块级操作
编辑器支持以下能力：
- 块新增、删除、复制、剪切、复制副本
- 拖拽重排
- 层级缩进 / 取消缩进
- 子块插入
- 连续多块选择
- 多块批量转换、剪切、删除、复制副本
- 折叠 / 展开子树
- 手动块高亮
- 自动保存

### 7.4 常用快捷与输入习惯
编辑器支持一批高频快捷方式：
- 输入 / 打开 Slash Commands
- 输入 #、##、>、-、1.、- [ ]、- [x]、$$、---、``` 自动识别为对应块类型
- Ctrl/Cmd + F 打开块内搜索
- Ctrl/Cmd + Enter 在下方插入新块
- Alt + Enter 在光标位置分裂块
- Alt + ArrowUp / ArrowDown 调整块顺序
- Tab / Shift+Tab 调整列表或待办层级
- Ctrl/Cmd + C / X / Shift + D 用于复制、剪切、复制副本

### 7.5 Markdown 与双链
当前编辑器支持以下内容语义：
- 内联 Markdown 样式解析，例如粗体、斜体、删除线、行内代码
- 使用 [[文档名]] 或 [[路径]] 创建文档双链
- 使用 [[路径#块ID]] 创建跨文档块引用，也支持同文档内块级引用补全
- 在编辑器中按住 Ctrl/Cmd 并点击 `[[...]]` 引用，可直接跳转到对应文档或目标块

### 7.6 网页剪藏
网页剪藏入口目前放在文档页的“辅助区 / Auxiliary panel”中。首版同时支持两条入口：
- 在桌面应用中直接输入网页 URL，剪藏到当前选中文档下面
- 通过本机桥接服务接收浏览器扩展发送的页面 payload

应用内 URL 剪藏的基本流程：
1. 先在文档树中选中一个父文档。
2. 如果右侧辅助区是收起状态，点击“展开辅助区 / Show auxiliary”。
3. 在“网页剪藏 / Web clip”输入框里粘贴目标 URL。
4. 点击“剪藏网页 / Clip webpage”。
5. 导入完成后，应用会自动跳转到新建的子文档。

当前剪藏结果会自动保留这些信息：
- 正文可读内容
- 封面图和正文内图片
- 来源 URL、来源站点、剪藏时间、发布时间、作者、状态、封面图等字段
- 若同一来源 URL + 同一内容哈希已存在，会直接复用已有文档而不是重复创建

块编辑器里会在对应文本块下方显示图片和链接预览；点击链接或图片卡片会走系统外链打开。

### 7.7 浏览器扩展桥接
如果希望浏览器里一键发送当前页面到桌面端，可使用仓库中的 [web-clip-extension/README.md](web-clip-extension/README.md) 扩展脚手架。使用步骤如下：

1. 打开“配置中心 / Settings”。
2. 在“浏览器桥接 / Browser bridge”区域启用本地桥接服务。
3. 确认监听端口，复制“扩展提交地址”和“授权令牌”。
4. 在 Chromium / Edge 中以“加载已解压扩展”的方式载入 `web-clip-extension` 目录。
5. 把刚才复制的 endpoint 和 Bearer token 填进扩展弹窗。
6. 在任意 `http/https` 页面点击扩展按钮执行剪藏。

桥接模式下，桌面端只监听 `127.0.0.1`，并使用 Bearer token 做鉴权。修改端口或重置令牌后，需要同步更新扩展里的配置。

## 8. 文档关系
在文档预览页底部可以看到三个关系面板：
- 子文档
- 出链
- 反向链接

你可以直接点击这些关系项，在相关文档之间来回跳转。

## 9. 知识图谱
主界面的“知识图谱 / Knowledge graph”区域会展示：
- 文档树结构边
- 文档之间的引用链接边

可用于快速查看当前工作区的知识结构和关系密度。

## 10. Database 结构化视图
当前数据库页已经拆成两个明确视图：
- 文档目录 / Document catalog：用于管理每篇文档的结构化字段、Table 与 Board
- 独立数据库 / Standalone databases：用于管理独立 database 的 schema 与 entity 行

进入 Database 页后，可以先用顶部切换按钮在这两个视图之间切换，再分别操作对应的数据模型。

### 10.1 自定义列
文档目录支持为每篇文档增加结构化字段。当前字段类型包括：
- 文本
- 单选
- 多选
- 日期
- 复选框

点击“新增列 / Add column”后，可定义：
- 列名
- 字段类型
- 选项（适用于单选 / 多选）

### 10.2 Table 视图
Table 视图适合批量查看和编辑文档元数据，例如：
- 状态
- 标签
- 时间点
- 是否完成
- 自定义文本说明

### 10.3 Board 视图
Board 视图适合看板式管理。
当前支持按以下维度分组：
- 父级文档
- 单选字段
- 多选字段
- 复选框字段
- 日期字段
- 文本字段

拖拽规则：
- 按父级分组时，拖拽会修改文档父级
- 按单选 / 复选框分组时，拖拽会直接改写该字段值
- 按多选分组时，拖拽到某列会把该选项加入字段；拖到空列会清空该字段
- 按日期分组时，拖拽会把该日期写回字段
- 按文本分组时，当前主要区分“空值 / 非空值”两类状态

### 10.4 独立 Database
切换到“独立数据库 / Standalone databases”视图后，可以：
- 点击“新增数据库 / Add database”创建独立 database
- 进入某个 database 后，为它单独新增字段
- 创建不绑定文档的 entity，也可以通过下拉直接关联到某篇已有文档
- 在 entity 卡片中直接修改字段值，也可以随时改绑或清空关联文档
- 在实体区域可切换卡片视图和表格视图；表格视图更适合横向比较多条实体并直接编辑字段
- 对独立 entity 列表按“全部字段 / 关联文档 / 某个字段”进行过滤，并按创建时间或更新时间排序
- 可以把当前过滤条件、排序方式和 cards / table 视图状态保存成命名 saved view，后续一键切回
- 可以直接勾选当前可见的独立 entity，执行批量删除
- 对当前已选实体批量应用某个字段值、批量清空某个字段值，并批量清空关联文档

现在独立 database 已经不再和文档 catalog 的 Table / Board 混在同一块主视图里；两套 schema 会分别显示、分别编辑。

## 11. AI 功能
### 11.1 配置 AI
在“配置中心 / Settings”中可以配置：
- 是否启用 AI
- Base URL
- Chat Model
- Embedding Model
- API Key
- 自动摘要

### 11.2 Ask AI
进入“AI 助手 / AI Assistant”页面后，可以围绕当前选中文档进行增强：
- 输入问题
- 可点击“查找相关笔记 / Find related notes”先做语义检索
- 再点击“询问 AI / Ask AI”发起问答

### 11.3 自动化
当前已经支持一类自动化：
- 文档保存时自动摘要（仅在当前摘要为空或仍为默认占位内容时触发）

还支持对当前文档手动执行“运行已启用自动化 / Run enabled automations”，当前会按设置执行摘要自动化。

## 12. 插件系统
### 12.1 插件来源
插件会从两个位置扫描：
- 工作区下的 plugins 目录
- 用户数据目录中的插件根目录

### 12.2 当前能力
插件当前可以：
- 贡献首页 dashboard cards
- 贡献文档级 actions
- 声明并持久化插件设置项
- 监听 workspace events
- 读取文档信息
- 更新摘要
- 写入插件事件

### 12.3 插件管理
进入“插件中心 / Plugins”页面后，当前支持：
- 启用 / 禁用
- 重载全部插件
- 单插件 Reload / Recover
- 在插件详情中直接编辑并保存插件设置
- 从本地文件夹安装插件
- 卸载用户级插件

补充说明：
- “安装文件夹 / Install Folder”会把本地插件复制到用户数据可写目录；如果目标是同 id 的用户级插件，当前支持替换升级。
- 如果你要安装的插件与工作区内已有插件同 id，当前会直接阻止安装，避免覆盖工作区插件。
- 当前版本还会根据插件 manifest 中的 `engines.knowbook` 做基础版本兼容校验；插件市场、远程安装与打包分发目前仍未开放。

仓库内已经包含 activity-pulse 示例插件，可直接作为参考。

## 13. 备份与导出
### 13.1 Markdown 备份
应用使用 SQLite 作为核心存储，同时支持将数据导出为树形 Markdown 目录。

你可以通过首页按钮手动触发：
- 立即执行备份 / Run backup now
- 恢复备份 / Restore backup

当前版本已经支持从备份目录恢复回 SQLite。当前恢复会优先以备份内容为准，恢复文档树、块树、默认文档 database 的列定义与字段值，以及独立 database 的列定义 / saved views / entities；同时会更新已有文档、补建缺失文档，并在同一恢复范围内删除备份中已不存在的旧文档或独立 database。

### 13.2 单文档导出
在文档预览区顶部可以：
- 复制 Markdown
- 导出 Markdown 文件

## 14. 数据位置
界面底部的“存储 / Storage”区域可查看：
- 数据库路径
- 备份根目录
- 最近一次备份时间
- 当前 AI 端点

这是排查环境和确认本地数据落盘情况的第一入口。

## 15. 推荐使用流程
建议按下面顺序使用 KnowBook：

1. 创建顶层文档和子文档，先建立知识树。
2. 在编辑器内用块结构整理正文、要点、待办和代码片段。
3. 为文档补充结构化字段，并在 Table / Board 中管理状态。
4. 如果需要更独立的结构化对象，可在 Database 页创建独立 database 和 entity。
5. 使用双链、反向链接和知识图谱查看关系网络。
6. 配置 AI 后，用语义检索和 Ask AI 做增强理解。
7. 打开自动摘要，降低重复整理成本。
8. 定期执行 Markdown 备份，保留可读、可迁移的本地副本。

## 16. 当前版本注意事项
当前版本仍有这些边界：
- 当前版本按首发边界交付：文档 catalog 使用 Table / Board，独立 database 使用 cards / table + saved views + 批量编辑，两条主线分开管理
- 独立 database 的专属 Board 视图与更强的 View 抽象明确不在首发版本范围内
- Board 已支持按 Date、Text 分组，但文本分组当前仍偏轻量，主要用于区分空值与非空值
- 跨文档块引用已可用，但更细粒度的块级关系能力仍会继续增强
- 插件首发范围以本地工作区 / 用户级安装、dashboard cards、document actions、workspace events 与 settings 为准；插件市场、远程安装、外部 SDK 不在首发范围内
- 当前版本已支持从 Markdown 备份目录恢复文档树与块树，并保留块级 id、层级、高亮与代码块语言；默认文档 database 与独立 database 的列定义 / 字段值 / saved views / entities 也会一并恢复，且会同步删除备份中已不存在的独立 database
- AI 自动化首发范围以摘要工作流为主，更多自动化能力会在后续版本继续增加
- 应用内更新只在打包后的桌面应用中可用；开发模式和未打包产物不会执行自动更新

## 17. 常见问题
### 17.1 为什么 AI 按钮不可用？
通常是以下原因：
- 尚未启用 AI
- 没有填写 API Key
- Prompt 为空
- 当前文档未选中

### 17.2 为什么开发模式里看不到应用更新？
这是当前设计行为：
- 自动更新只在打包后的桌面应用里启用
- 开发模式、预览模式和未打包产物会显示“当前环境不支持自动更新”
- 如果你要验证更新链路，应先运行 `npm run dist` 或使用正式安装包

### 17.3 为什么 Board 拖拽后内容变了？
这是当前设计的一部分：
- 如果按父级分组，拖拽会修改父级
- 如果按字段分组，拖拽会修改该字段值

### 17.4 为什么插件没有出现？
可以按顺序检查：
- 插件目录是否放在 plugins 下
- 插件 manifest 是否正确
- 左侧插件区是否显示错误状态
- 先尝试 Reload / Recover

## 18. 快捷键速查
下面是当前版本最常用的一组快捷键与输入约定：

### 18.1 全局与导航
- Ctrl+K：打开全局搜索
- 点击任意页面导航图标：在文档、总览、Database、图谱、AI、插件、设置之间切换

### 18.2 编辑器快捷键
- /：打开 Slash Commands
- Ctrl/Cmd + F：打开块内搜索
- Ctrl/Cmd + Enter：在当前块下方插入新块
- Alt + Enter：在光标位置分裂当前块
- Alt + ArrowUp / ArrowDown：上下移动当前块
- Tab / Shift+Tab：调整列表或待办的层级
- Ctrl/Cmd + C：复制当前块或当前选区
- Ctrl/Cmd + X：剪切当前块或当前选区
- Ctrl/Cmd + Shift + D：复制当前块副本

### 18.3 常见输入约定
- `#`、`##`：自动识别为标题
- `>`：自动识别为引用块
- `-`、`1.`：自动识别为列表
- `- [ ]`、`- [x]`：自动识别为待办
- `$$`：自动识别为数学块
- `---`：自动识别为分隔线
- 输入三个反引号：自动识别为代码块
- `[[文档名]]`、`[[路径]]`：创建文档双链
- `[[路径#块ID]]`：创建跨文档块引用

### 18.4 使用提醒
- 多块选择、块拖拽、批量转换等复杂操作已经可用，但当前仍建议在重要内容上配合 Markdown 备份使用。
- 不同平台上的 `Ctrl/Cmd` 会自动对应当前系统的 Control 或 Command 键。

## 19. AI 配置示例
如果你使用兼容 OpenAI 风格的 API，可以按下面的方式理解字段：

### 19.1 常见字段含义
- Base URL：模型服务入口，例如 `https://api.openai.com/v1`
- Chat Model：问答与摘要用的模型名，例如 `gpt-4.1-mini`
- Embedding Model：语义检索用的向量模型，例如 `text-embedding-3-small`
- API Key：对应服务的访问凭证
- 自动摘要：开启后，保存文档时会在摘要为空或默认占位时自动生成摘要

### 19.2 示例配置
下面是一组示例值，仅用于说明字段格式：

```text
Enable AI: On
Base URL: https://api.openai.com/v1
Chat Model: gpt-4.1-mini
Embedding Model: text-embedding-3-small
API Key: sk-...
Auto summary: On
```

### 19.3 推荐检查顺序
如果 AI 功能没有按预期工作，建议依次检查：
- 是否已在设置页启用 AI
- API Key 是否已填写
- Base URL 是否包含正确的 `/v1` 前缀或服务根路径
- Chat Model 与 Embedding Model 是否都存在于你的服务端
- 当前是否已经选中一篇文档

## 20. 数据目录与备份恢复说明
### 20.1 本地数据主要放在哪里
应用会把核心数据放在 Electron 的 userData 目录下。当前版本主要会写入这些位置：
- `storage/knowbook.db`：SQLite 主数据库
- `backups/markdown/`：树形 Markdown 备份目录
- `plugins/`：用户级插件目录

你不需要手动寻找这些目录；在应用底部的“存储 / Storage”区域可以直接看到数据库路径和备份根目录的绝对路径。

### 20.2 备份文件长什么样
- 每篇文档会按文档路径导出为 `.md` 文件
- Markdown 文件会包含 frontmatter，例如文档 id、标题、路径、更新时间、摘要，以及默认文档 database 的列定义和当前文档字段值
- 独立 database 会额外导出保留的元数据 Markdown 文件，其中包含该 database 的列定义、saved views 与 entities；并带有一个 manifest 文件用于声明当前备份包含的独立 database 集合
- 正文部分会按当前块结构序列化为 Markdown

### 20.3 当前恢复能力说明
当前版本已经支持通过图形入口从 Markdown 备份目录恢复回 SQLite；入口位于首页和设置页的存储区域。

这意味着：
- Markdown 备份当前首先是可读、可迁移、可审计的导出副本
- 恢复会优先采用备份中的文档树与块树，解决同路径占位冲突，并在当前恢复范围内删除备份中已不存在的旧文档
- 当前恢复已经保留块级 id、层级、高亮、代码块语言，以及默认文档 database 的列定义与字段值、独立 database 的列定义 / saved views / entities，并会删除备份中已不存在的独立 database
- 删除同步之外的更细粒度高级冲突策略仍在继续补齐，但不属于首发版本范围
- 在进行大规模整理、批量拖拽或结构调整前，建议先手动运行一次备份

### 20.4 推荐备份习惯
- 在重要编辑前手动执行“立即执行备份 / Run backup now”
- 定期复制数据库文件和 Markdown 备份目录到外部位置
- 如果你依赖用户级插件，也建议一并备份 userData 下的 `plugins/` 目录

## 21. 插件开发说明（首版）
当前版本最适合用简单 JavaScript 插件快速扩展工作区行为。

### 21.1 最小目录结构
一个最小插件目录通常包含：

```text
my-plugin/
	plugin.json
	index.js
```

### 21.2 manifest 示例

```json
{
	"id": "my-plugin",
	"name": "My Plugin",
	"version": "1.0.0",
	"description": "Example KnowBook plugin",
	"author": "Your Name",
	"entry": "index.js",
	"enabledByDefault": true,
	"engines": {
		"knowbook": ">=0.1.2"
	}
}
```

当前常用字段包括：
- `id`：插件唯一标识
- `name`：显示名称
- `version`：插件版本
- `entry`：入口文件，默认一般是 `index.js`
- `enabledByDefault`：是否默认启用
- `engines.knowbook`：可选，用于声明与当前 KnowBook 版本的兼容范围

示例里的版本号只用于说明格式；实际发布插件时，应按你实际测试过的最低 KnowBook 兼容版本填写 `engines.knowbook`。

### 21.3 入口文件示例

```js
module.exports.activate = function activate(api) {
	const card = api.contributeDashboardCard({
		id: 'hello-card',
		title: 'Plugin loaded',
		body: 'Hello from KnowBook plugin.'
	})

	api.onWorkspaceEvent('document.updated', function handleEvent(event) {
		card.update({
			title: 'Latest event',
			body: `Observed: ${event.type}`
		})
	})

	api.contributeDocumentAction(
		{
			id: 'touch-summary',
			label: 'Refresh summary'
		},
		function runAction(context) {
			api.documents.updateSummary(context.document.id, 'Updated by plugin')
			return {
				message: 'Summary updated.',
				refreshDocument: true
			}
		}
	)
}
```

### 21.4 当前可用能力
首版插件 API 主要支持：
- 贡献首页 dashboard card
- 贡献文档级 action
- 监听 workspace events
- 读取当前文档详情
- 更新文档摘要
- 写入插件事件日志

### 21.5 当前边界
- 当前更适合直接写 JavaScript 入口文件
- 仓库里已有 TypeScript 风格的内部 API / 基类，但外部可直接安装的 Typed SDK 还没有单独发布
- 当前还没有插件市场与统一打包分发流程
- 如果只是试验能力，最简单的方式是参考仓库内的 `plugins/activity-pulse/`

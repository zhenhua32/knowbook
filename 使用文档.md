# KnowBook 使用文档

## 1. 产品说明
KnowBook 是一个本地优先的知识管理客户端，核心形态是“文档树 + 块编辑器 + 结构化数据库 + 知识图谱 + AI 自动化 + 插件宿主”。

当前版本适合以下场景：
- 个人知识库与项目笔记整理
- 具备层级结构的块级写作
- 为文档补充结构化字段并用 Table / Board 管理
- 基于本地 SQLite 的长期沉淀与 Markdown 备份
- 通过 AI 对文档做问答、检索和摘要增强

## 2. 启动方式
### 2.1 开发模式
在项目根目录执行：

```bash
npm install
npm run dev
```

### 2.2 构建产物
在项目根目录执行：

```bash
npm run build
```

构建完成后，Electron 主进程、Preload、Renderer 产物会输出到 out 目录。

## 3. 首次进入后先做什么
建议第一次打开后先完成下面 4 步：

1. 在左侧栏找到“界面语言 / Interface language”，切换为中文或 English。
2. 进入“配置中心 / Settings”，填写 Base URL、Model、Embedding model、API Key，并按需打开自动摘要。
3. 在工作区树中创建一篇根文档，进入编辑态开始录入内容。
4. 用“立即执行备份 / Run backup now”测试一次 Markdown 备份是否正常生成。

## 4. 界面结构
主界面大致分为两部分：

### 4.1 左侧侧栏
左侧主要负责页面导航与全局入口：
- 文档页
- 总览页
- Database 页
- 图谱页
- AI 助手页
- 插件中心页
- 配置中心页

### 4.2 主内容区
主内容区包含以下模块：
- 工作区总览与统计卡片
- 工作区文档树
- 文档预览 / 编辑面板
- Database 的 Table / Board / 独立 entity 视图
- 知识图谱
- AI 问答与语义检索结果
- 插件管理与配置面板
- 存储信息、语言、AI 与备份设置

## 5. 语言切换
当前版本已支持中文和英文界面切换。

操作方法：
1. 打开左侧侧栏顶部的“界面语言 / Interface language”。
2. 在下拉框中选择“中文”或“English”。
3. 选择后会立即生效，并自动保存到本地设置。
4. 下次重新打开应用时，会保留上次选择的语言。

## 6. 文档与工作区树
### 6.1 新建文档
- 点击工作区树右上角“新建根文档 / New root”可创建顶层文档。
- 在文档预览面板点击“新增子文档 / Add child”可创建当前文档的子文档。

### 6.2 移动文档
支持两种方式：
- 在工作区树中拖拽文档到另一个文档下，完成重挂父级。
- 在预览面板使用“移动到 / Move to”下拉框，把当前文档移动到指定父级或根目录。

### 6.3 收藏文档
在预览面板顶部点击星标按钮，可将文档加入收藏区，便于快速打开。

### 6.4 全局搜索
点击工作区树区域的搜索按钮，或使用快捷键 Ctrl+K：
- 可搜索文档标题
- 可搜索块内容
- 搜索结果可直接跳转到对应文档

## 7. 文档编辑器
### 7.1 基本编辑
进入编辑模式后，可以修改：
- 标题
- 摘要
- 文档中的所有块内容

### 7.2 块类型
当前支持的块类型包括：
- 文本段落
- 一级标题
- 二级标题
- 待办事项
- 代码块
- 数学公式
- 引用块
- 无序列表
- 有序列表
- 分隔线

### 7.3 块级操作
编辑器支持以下能力：
- 块新增、删除、复制、剪切、复制副本
- 拖拽重排
- 层级缩进 / 取消缩进
- 子块插入
- 连续多块选择
- 多块批量转换
- 折叠 / 展开子树
- 标签过滤
- 自动保存

### 7.4 常用快捷与输入习惯
编辑器支持一批高频快捷方式：
- 输入 / 打开 Slash Commands
- 输入 #、##、>、-、1.、- [ ]、- [x]、$$、---、``` 自动识别为对应块类型
- Ctrl/Cmd + F 打开块内搜索
- Ctrl/Cmd + Enter 在下方插入新块
- Alt + Enter 在光标位置分裂块
- Alt + ArrowUp / ArrowDown 调整块顺序
- Tab / Shift+Tab 调整列表或待办层级
- Ctrl/Cmd + C / X / Shift + D 用于复制、剪切、复制副本

### 7.5 Markdown 与双链
当前编辑器支持以下内容语义：
- 内联 Markdown 样式解析，例如粗体、斜体、删除线、行内代码
- 使用 [[文档名]] 或 [[路径]] 创建文档双链
- 使用 [[路径#块ID]] 创建跨文档块引用，也支持同文档内块级引用补全
- 在编辑器中按住 Ctrl/Cmd 并点击 `[[...]]` 引用，可直接跳转到对应文档或目标块

## 8. 文档关系
在文档预览页底部可以看到三个关系面板：
- 子文档
- 出链
- 反向链接

你可以直接点击这些关系项，在相关文档之间来回跳转。

## 9. 知识图谱
主界面的“知识图谱 / Knowledge graph”区域会展示：
- 文档树结构边
- 文档之间的引用链接边

可用于快速查看当前工作区的知识结构和关系密度。

## 10. Database 结构化视图
当前数据库页包含两类能力：
- 基于文档目录的 catalog 结构化视图
- 独立 database / entity 的首版管理界面

### 10.1 自定义列
文档目录支持为每篇文档增加结构化字段。当前字段类型包括：
- 文本
- 单选
- 多选
- 日期
- 复选框

点击“新增列 / Add column”后，可定义：
- 列名
- 字段类型
- 选项（适用于单选 / 多选）

### 10.2 Table 视图
Table 视图适合批量查看和编辑文档元数据，例如：
- 状态
- 标签
- 时间点
- 是否完成
- 自定义文本说明

### 10.3 Board 视图
Board 视图适合看板式管理。
当前支持按以下维度分组：
- 父级文档
- 单选字段
- 多选字段
- 复选框字段
- 日期字段
- 文本字段

拖拽规则：
- 按父级分组时，拖拽会修改文档父级
- 按单选 / 复选框分组时，拖拽会直接改写该字段值
- 按多选分组时，拖拽到某列会把该选项加入字段；拖到空列会清空该字段
- 按日期分组时，拖拽会把该日期写回字段
- 按文本分组时，当前主要区分“空值 / 非空值”两类状态

### 10.4 独立 Database
除了文档 catalog 视图外，数据库页还支持创建独立数据库：
- 点击“新增数据库 / Add database”可创建独立 database
- 进入某个 database 后，可以新增自定义列
- 可以创建不绑定文档的 entity，也可以把 entity 关联到某篇文档
- 已创建的 entity 支持直接编辑字段值

当前这一部分已经可用，但整体产品形态仍在持续收口中；你会同时看到独立 database 与基于文档目录的结构化视图并存。

## 11. AI 功能
### 11.1 配置 AI
在“配置中心 / Settings”中可以配置：
- 是否启用 AI
- Base URL
- Chat Model
- Embedding Model
- API Key
- 自动摘要

### 11.2 Ask AI
进入“AI 助手 / AI Assistant”页面后，可以围绕当前选中文档进行增强：
- 输入问题
- 可点击“查找相关笔记 / Find related notes”先做语义检索
- 再点击“询问 AI / Ask AI”发起问答

### 11.3 自动化
当前已经支持一类自动化：
- 文档保存时自动摘要

还支持对当前文档手动执行“运行已启用自动化 / Run enabled automations”，当前会按设置执行摘要自动化。

## 12. 插件系统
### 12.1 插件来源
插件会从两个位置扫描：
- 工作区下的 plugins 目录
- 用户数据目录中的插件根目录

### 12.2 当前能力
插件当前可以：
- 贡献首页 dashboard cards
- 贡献文档级 actions
- 监听 workspace events
- 读取文档信息
- 更新摘要
- 写入插件事件

### 12.3 插件管理
进入“插件中心 / Plugins”页面后，当前支持：
- 启用 / 禁用
- 重载全部插件
- 单插件 Reload / Recover
- 从本地文件夹安装插件
- 卸载用户级插件

另外，当前版本还会根据插件 manifest 中的 `engines.knowbook` 做基础版本兼容校验。

仓库内已经包含 activity-pulse 示例插件，可直接作为参考。

## 13. 备份与导出
### 13.1 Markdown 备份
应用使用 SQLite 作为核心存储，同时支持将数据导出为树形 Markdown 目录。

你可以通过首页按钮手动触发：
- 立即执行备份 / Run backup now

### 13.2 单文档导出
在文档预览区顶部可以：
- 复制 Markdown
- 导出 Markdown 文件

## 14. 数据位置
界面底部的“存储 / Storage”区域可查看：
- 数据库路径
- 备份根目录
- 最近一次备份时间
- 当前 AI 端点

这是排查环境和确认本地数据落盘情况的第一入口。

## 15. 推荐使用流程
建议按下面顺序使用 KnowBook：

1. 创建顶层文档和子文档，先建立知识树。
2. 在编辑器内用块结构整理正文、要点、待办和代码片段。
3. 为文档补充结构化字段，并在 Table / Board 中管理状态。
4. 如果需要更独立的结构化对象，可在 Database 页创建独立 database 和 entity。
5. 使用双链、反向链接和知识图谱查看关系网络。
6. 配置 AI 后，用语义检索和 Ask AI 做增强理解。
7. 打开自动摘要，降低重复整理成本。
8. 定期执行 Markdown 备份，保留可读、可迁移的本地副本。

## 16. 当前版本注意事项
当前版本仍有这些边界：
- 独立 database 已经可用，但当前仍与基于文档目录的 catalog 视图并存，整体产品边界还在继续收口
- Board 已支持按 Date、Text 分组，但文本分组当前仍偏轻量，主要用于区分空值与非空值
- 跨文档块引用已可用，但更细粒度的块级关系能力仍会继续增强
- 插件市场、外部 SDK、打包分发与更多 UI 扩展位仍在补强
- AI 自动化当前以摘要工作流为主，更多自动化能力会在后续版本继续增加

## 17. 常见问题
### 17.1 为什么 AI 按钮不可用？
通常是以下原因：
- 尚未启用 AI
- 没有填写 API Key
- Prompt 为空
- 当前文档未选中

### 17.2 为什么 Board 拖拽后内容变了？
这是当前设计的一部分：
- 如果按父级分组，拖拽会修改父级
- 如果按字段分组，拖拽会修改该字段值

### 17.3 为什么插件没有出现？
可以按顺序检查：
- 插件目录是否放在 plugins 下
- 插件 manifest 是否正确
- 左侧插件区是否显示错误状态
- 先尝试 Reload / Recover

---
如需面向最终用户继续补文档，下一步最值得补的是：
- 快捷键总表
- 插件开发说明
- AI 配置示例
- 数据目录与备份恢复说明
